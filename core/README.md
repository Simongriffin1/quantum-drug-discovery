# PeptideForge core

Python package `peptideforge`. Contracts only in P1 — implementations follow P2–P12.

```bash
poetry install
poetry run pytest
poetry run mypy src
poetry run python -m peptideforge.contracts.export_schemas
```
